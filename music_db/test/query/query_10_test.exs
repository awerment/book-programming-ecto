defmodule Query10Test do
  @moduledoc """
  File is only included for completeness' sake.
  The query_10 examples contain two queries to show the benefits of composability.
  The actual composition of queries is shown in query_11
  """
end
